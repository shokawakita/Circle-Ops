# circle-ops — Claude への常時前提

このリポジトリは、神奈川・東京を中心に活動する独立サークルの運営リポジトリです。
Claude Code はここを作業の起点として、イベント設計・告知・収支・振り返り・発信を担当します。

## 団体の前提

- 形態: 個人事業主（青色申告済み）。将来的に合同会社を検討
- 実績: 通算1000人規模。ボードゲーム会を月4回、平均13名、1回あたり黒字8,000円で継続中
- 集客: つなげーと（イベント作成）。note・つなげーとアプリでブログを定期投稿
- 数値管理: Notion で確定申告用・分析用のデータを運用中（**作り直さず拡張する**）
- クローズ会: つなげーと外で、関係のできた人同士の会を不定期開催（主催者育成の練習台）
- 目指す形: レーベル型（主催者が自分のブランドを掲げ、いずれ独立できる体制）
- 活動エリア: 神奈川・東京（横浜 / 川崎 / 新宿 を主軸）
- 定番イベント: ボードゲーム会、ランチカフェ
- 特別企画: 珍しいカフェ巡り、コンセプトご飯会、カラオケ大会、鍋パーティ、季節行事、スポーツ大会、ウォーキング
- スタンス: 参加者の体験の質を最優先し、その上で黒字化を目指す

詳細は `brand/concept.md` を参照してください。

## 出力する文章のルール

- トンマナは `brand/voice.md` に従う。例外なく毎回参照すること
- 募集文を作るときは `brand/rules.md` の参加規約とキャンセルポリシーを必ず差し込む
- 参加者に向けた文章では、煽り・誇張・「絶対に」「必ず」といった断定を使わない
- 「出会い」「恋活」「婚活」という語を看板に使わない（後述の法務メモ参照）

## 絶対に守るルール

1. **参加者の個人情報をこのリポジトリに入れない。**
   扱ってよいのはニックネーム・参加回数・写真掲載可否まで。
   本名・電話番号・メールアドレス・住所・勤務先は、申込フォーム側に置いたままにする。
   誤って含まれているファイルを見つけたら、作業を止めて指摘すること。

2. **法務・税務の断定をしない。**
   `docs/` の資料は一般的な情報の整理であり、法的助言ではない。
   金銭の授受、法人化、契約に関わる判断が出てきたら「専門家に確認」を添える。

3. **金額・日付・人数を勝手に確定しない。**
   不明な値は `TODO:` として残し、埋めた体で進めない。

## 主要ファイル

| パス | 中身 |
|---|---|
| `brand/concept.md` | コンセプト・ターゲット・やらないこと |
| `brand/voice.md` | 文体・語彙・NG表現 |
| `brand/rules.md` | 参加規約（募集文に毎回差し込む） |
| `venues/venues.md` | 会場台帳。原価計算と会場選定の元データ |
| `events/` | 1イベント1ディレクトリ。`_template/` を複製して使う |
| `finance/profit-share.md` | 利益分配とレーベル型手数料のルール |
| `finance/ledger.csv` | 収支台帳 |
| `docs/` | Vol.1 / Vol.2 / **Vol.3（現在の方針・移行計画）** |

## よく使うコマンド

- `/event-new` — 企画から募集文・台本・収支シミュレーションまで一括生成
- `/event-close` — 開催後の締め。収支確定・振り返り・レポ下書き
- `/sns-week` — 週次のSNS投稿カレンダー
- `/kpi-monthly` — 月次サマリと収支レポート

## イベント作成の4パターン

実際の運営はこの4つで回っています。自動化の設計は `docs/automation-plan.md` を参照。

| パターン | 手順 |
|---|---|
| つなげーと_既存イベントの再度作成（月4回・最頻出） | 場所取り → つなげーとページ → Notionページ →（日本酒のみ）Threads告知 |
| つなげーと_新規イベントの作成 | 場所取り → 予算作成 → つなげーとページ → Notionページ |
| シェアハウス_イベントの作成（協賛金申請用） | 予算作成・参加者調整 → Googleform申請 → Notionページ →（開催後）SNS投稿＋レシート＋参加者連携 |
| 身内イベントの作成 | TODO: 手順を確認する |

## Notion 連携

**既存の Notion 運用（確定申告用・分析用）は作り直さない。不足分だけを足す。**
Claude Code 側は「作る」担当、Notion 側は「溜める」担当と分ける。

DBはすでに構築済み。**新しく作らず、既存のDBを読み書きすること。**

| DB | 役割 |
|---|---|
| `TEST_Event_Projects_DB` | イベント本体。**単一の正** |
| `Tickets_DB` | チケット（価格 / 定員 / 支払方法） |
| `Venues_DB` | 会場（所在地 / 収容人数 / 利用料金 / 使用可能時間帯） |
| `Event Costs_DB` | 支出管理。経費精算とイベント別支出 |
| `Event_Task_DB` | 37個の共通タスク（`01_01_`〜`11_02_`）。複製して使う |
| `Participants_DB`（参加者・関係者） | 参加者。**ニックネームのみ。本名・連絡先は入れない** |

### `ep_*` プロパティは使わない

`TEST_Event_Projects_DB` の `ep_` プレフィックスのプロパティは、
**イベントプラットフォームのアプリ作成時に使ったもので、現在の運用では使いません。**

- `ep_*` を**読まない・書かない**
- `ep_*` 以外のプロパティ、ページ本文、リレーション先のDBで組み立てる
- 新しくプロパティを足すときも `ep_` を付けない

使うのは `イベント項目` / `イベントタイプ` / `開催予定期間` / `確定日時` / `会場情報` /
`場所詳細` / `チケット一覧` / `参加窓口` / `進捗状況` / `詳細` / `開催年度` / `開催月` /
`確定申告判定` / `関連タスク` / `参加者・関係者` / `関連サークル` / `関連資料` / `優先度`。

募集本文はプロパティではなく**ページ本文**に置きます。

## つなげーと連携

- サークルURL: https://tunagate.com/circle/97956 （サークルID `97956`）
- API仕様のまとめ: `docs/tunagate-api.md`
- 認証トークンは環境変数 `TUNAGATE_API_TOKEN`（`.env`。`.gitignore` 済み）。
  **リポジトリにも会話にも書かない**

APIは **下書き作成 → 公開** の2段階です。
**Claude が実行してよいのは下書き（`POST /api/external/events`）まで。**
公開（`POST /api/external/events/:id/publish`）は、人が内容を確認してからにする。
Googleフォーム送信・SNS投稿も同じく、下書きを作って止まる。

作成前に必ず確認すること。

- 下書きは**10件まで**。超えると 422 で落ちる
- プランを省略すると**無料プラン1件が自動生成される**。有料回では必ずプランを渡す
- `main_image_url` の取得失敗は**リクエスト全体を422にする**。
  Notion添付の署名付きURLは失効するため渡さない
